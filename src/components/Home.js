import React from "react";
import { username, city } from "../data/user"; // 👈 import the variables

function Home() {
  return (
    <div id="home">
      <h1>
        Jennifer Nyambura is a Web Developer from Thika, Kenya.
      </h1>
    </div>
  );
}

export default Home; // 👈 export the component
